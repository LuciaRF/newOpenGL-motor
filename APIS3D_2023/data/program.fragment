#version 330

uniform sampler2D colorText;

//out vec4 fragColor;

in vec4 fcolor;
in vec2 ftextcoord;

void main()  
{  
	//fragColor=vec4(1.0f,1.0f,1.0f,0.0f);

	gl_FragColor = texture2D(colorText,ftextcoord);
} 